/** 
* @brief header file for converting types into strings and back
*/
#include <string>
#include <type_traits>

template <typename T = int>
constexpr auto intToString(std::string &&value) -> std::string&{
  
}
